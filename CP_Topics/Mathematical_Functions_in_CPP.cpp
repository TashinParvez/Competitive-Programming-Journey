#include <bits/stdc++.h>
using namespace std;

int main()
{
    /*

    abs(x)	    Returns the absolute value of x
    fabs(x) :   Returns the absolute value of any number(float)

            // fabs() function is used for floating values
            // abs() function is used for integer values


    floor(x)    Returns value lesser or equal to argument passed
    ceil(x)	    Returns the smallest integer as double not less than the argument


    round(x)    round (rounds a number)
                round(2.4) -> 2
                round(2.6) -> 3


    pow(x, y)   base and exponent
    cbrt(x) 	Returns the cube root of x (x^3)
    sqrt(x)     Return its square root value


    */

    return 0;
}
