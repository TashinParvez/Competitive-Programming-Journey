#include <stdio.h>
int main(){
    float pie = 3.1416;
    int r;
    float sum;
    printf("What is the value of the circle radious? \n");
    scanf("%d", & r);
    printf("Area of the circle is %f", sum = pie * r * r );
    return 0;
}